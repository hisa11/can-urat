このプログラムはnucleo f446re向けのuartを使用した配列相互非同期通信です。
名前の通り送信と受信を別々に行います。
受信されたデータは送信時と同じく配列に格納されて出力されます。

注意:このプログラムでは一つの配列の配列の送受信しかできません。
    複数の配列を送信すると受信側でどの配列が送信されたか分からなくなります。
    送信時に２つの配列を一つにし送信し、受信側で分けるなどの対策をしてください。